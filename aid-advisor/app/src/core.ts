// Single bridge to the shared analysis core. Everything in the app imports
// core types/values from here so the relative path to ../src/core lives in
// exactly one place.
export * from '../../src/core';
